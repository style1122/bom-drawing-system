# 2026-09-01 工作日志

## Git 仓库卫生清理

**背景**：用户要求把项目初始化为 git 仓库，检查发现项目早已是 git 仓库（main 分支，远程 origin=github.com/style1122/bom-drawing-system.git，已有 3 个提交），但缺少 .gitignore，构建产物被大量跟踪。

**问题数据**：
- `bom-frontend/node_modules` 被跟踪 48,915 个文件
- `bom-frontend/dist` 被跟踪 26 个文件
- 各模块 `target/` 被跟踪 152 个文件
- `.workbuddy/memory/*.md`（4 个本地工作日志）也被跟踪

**处理**：
1. 新建根目录 `.gitignore`（忽略 node_modules/、dist/、target/、*.class、IDE 配置、日志、.DS_Store、.workbuddy/、uploads/）
2. `git rm -r --cached` 取消跟踪 node_modules / dist / 各模块 target / .workbuddy（仅动索引，本地文件保留）

**结果**：node_modules/dist/target 跟踪数全部归零。工作区现存：12 个未跟踪新文件（ERP 订阅相关源码 ErpSubscriptionService/ErpSubscribeController/ErpSyncCursor、sql/05_erp_sync_cursor.sql、docs、.gitignore）+ 18 个已修改源码文件。未提交，等待用户决定。

## 提交与推送（未完成，被网络阻断）

**本地提交已完成**（2 个新提交）：
1. `2a90982` chore: 添加.gitignore并移除node_modules/dist/target等构建产物跟踪
2. `43ef5ec` feat: ERP订阅同步、图纸批量上传图号匹配及相关优化
（未提交：docs/_jstack.txt 调试文件，有意排除）

**远程仓库状态**：origin/main 已被重置为孤立提交 `1065dae`（仅含一个空的 README.md），与本地历史（a0ad3d5→af1201a→新提交）无共同祖先。需 force push。

**推送阻断原因**：WorkBuddy 注入的代理 `http://127.0.0.1:64965` 持续故障（github.com 域 CONNECT 隧道 502/000，api.github.com 正常 200）；直连 GitHub 被网络阻断（Empty reply/超时）。多次轮询重试均失败。

**遗留问题**：仓库 .git 体积 181MB，历史提交（a0ad3d5/13551f8/af1201a）含 31MB war 包、node_modules 大文件（skia 26MB 等）。即使网络恢复，推送也可能因传输量大而不稳定，建议后续用 git filter-repo 重写历史瘦身（远程已被重置，重写历史无外部影响）。

**待办**：网络恢复后执行 `GCM_INTERACTIVE=never git push --force origin main`。
